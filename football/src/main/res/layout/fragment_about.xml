<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingBottom="@dimen/activity_vertical_margin"
                android:paddingLeft="@dimen/activity_horizontal_margin"
                android:paddingRight="@dimen/activity_horizontal_margin"
                android:paddingTop="@dimen/activity_vertical_margin"
                tools:context="barqsoft.footballscores.AboutActivity$PlaceholderFragment">
    <!-- Doesn't really make sense to use paddingStart/End since it's the same padding in both-->

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/about_text_description"
        android:gravity="center"
        android:text="@string/about_text"
        android:textSize="12sp"
        />

</RelativeLayout>
